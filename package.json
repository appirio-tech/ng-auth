{
  "name": "appirio-tech-ng-auth",
  "version": "0.0.0",
  "main": "./src/src.coffee",
  "devDependencies": {
    "appirio-tech-webpack-config": "0.x.x",
    "npm-prepublish": "^1.2.1"
  },
  "dependencies": {
    "angular": "1.4.x",
    "angular-cookies": "^1.5.1",
    "angular-jwt": "0.0.x",
    "angular-resource": "1.4.x",
    "angular-storage": "0.0.x",
    "angular-ui-router": "0.2.x"
  },
  "scripts": {
    "example": "webpack-dev-server -d --colors",
    "dev": "webpack-dev-server -d --colors --dev",
    "build": "webpack --bail --progress -p --build"
  }
}
